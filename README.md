# MHE Scheduler v6
Run: `npm install` then `npm run dev`.
The Gantt date header is sticky, task bubbles render directly below dates, the task column is sticky, and the bottom scrollbar follows the visible chart.
